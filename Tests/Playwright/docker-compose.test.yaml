services:
  fuse-app:
    build:
      context: ../..
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:${FUSE_TEST_PORT:-5099}:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
    volumes:
      - test-data:/app/data
volumes:
  test-data:
